# shared/miniflow/pyproject.toml
[project]
name = "miniflow"
version = "0.1.0"
description = "Lightweight workflow engine for kidding services"
authors = [
    {name = "snaiws", email = "lghmk2@gmail.com"}
]
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

# 패키지 구조 명시
[tool.hatch.build.targets.wheel]
packages = ["src/miniflow"]